import ChatHeader from "./ChatHeader";

interface IMainBarProps extends React.PropsWithChildren {}

const MainBar: React.FunctionComponent<IMainBarProps> = (
  props
): JSX.Element => {
  return (
    <div className="p-2">
    </div>
  );
};

export default MainBar;
